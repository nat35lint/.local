#!/bin/bash

rm -rf ~/.config/qtile


cp -r ~/.local/backups/qtileog ~/.config/

cd ~/.config

mv qtileog qtile

cd

rm -rf ~/.config/kitty

cp -r ~/.local/backups/kittyog ~/.config/

cd ~/.config

mv kittyog kitty

cd

rm -rf ~/.config/cava

cd
